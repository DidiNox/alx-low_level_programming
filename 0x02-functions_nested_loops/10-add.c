#include "main.h"
#include <stdio.h>
/**
 * add - adds two integers.
 * @i: an integer
 * @j: an integer.
 *
 * Return: the result.
 */
int add(int i, int j)
{
	int result = i + j;

	return (result);
}
